#include<stdio.h>
int main()
{
    int mod, zero_count = 0;
    while(Number > 0)
    {
        /* code */
        mod = Number % 10;
        if (mod == 0) zero_count++;
        else break;
        Number = Number / 10;

    }
 return 0;   
}